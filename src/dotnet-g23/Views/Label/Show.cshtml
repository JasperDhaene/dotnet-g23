@using System.Threading.Tasks
@using dotnet_g23.Models.Domain
@using dotnet_g23.Models.Domain.State

@{
    ViewData["Title"] = "Label toekennen";
}
@model dotnet_g23.Models.ViewModels.LabelViewModels.ShowViewModel

<div class="content">
    <div class="container-fluid">
        @Html.Partial("_Messages")
        <div class="row">
            <div class="col-lg-12 m-t-3">
                <h1>Bedrijf '@Model.Company.Name'</h1>
                
                <p>U kunt de bevestinging van het Goed Bezig label doorsturen naar de volgende contactpersonen.</p>
                
                @if (Model.Contacts == null || !Model.Contacts.Any())
                {
                    <div class="alert alert-info m-t-1"><i class="fa fa-info-circle"></i> Geen contacten gevonden.</div>
                }
                @foreach (var contact in Model.Contacts)
                {
                    <div class="card">
                        <form asp-controller="Label" asp-action="Send" asp-route-id="@contact.ContactId" method="post">
                            @contact.Title @contact.FirstName @contact.LastName (@contact.Function)
                            <small>@contact.Email</small>
                            <a href="#" onclick="$(this).closest('form')[0].submit();" class="btn btn-secondary btn-md f-sm-right">Verzenden</a>
                        </form>
                    </div>
                }
            </div>
        </div>
    </div>
</div>